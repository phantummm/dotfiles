# Path to your oh-my-zsh installation.
export ZSH=$HOME/.oh-my-zsh

ZSH_THEME="nanotech"
plugins=(git zsh-syntax-highlighting)
source $ZSH/oh-my-zsh.sh

if [ -d /Users/alex ]; then
  export PATH="/Users/alex/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/share/npm/bin"
fi

export EDITOR="vim"

# yarn
export PATH="$PATH:`yarn global bin`"

# rbenv shims
export PATH="$HOME/.rbenv/bin:$PATH"''
if which rbenv > /dev/null; then eval "$(rbenv init -)"; fi

# pyenv et al.
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"

# asdf version-manager
. $HOME/.asdf/asdf.sh
. $HOME/.asdf/completions/asdf.bash

# aliases
alias cdn='cd ~/source/nutshell'
alias cdncs='cd ~/source/nutshell/client/client_modules/nutshell-client-shared'
alias cdnce='cd ~/source/nutshell/client-extensions'
alias cdf='cd ~/flux'
alias cdth='cd ~/p/gruvbox-dark-ui'
alias cdr='cd ~/r'
alias cdd='cd ~/r/dotfiles'

# work stuff
if [[ -d "$HOME/nub" ]]; then
	eval "$($HOME/nub/bin/nub init -)"
fi

[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
